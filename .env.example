# --- Database -------------------------------------------------------------
DATABASE_URL="postgresql://postgres@127.0.0.1:5432/montra?schema=public"

# --- Site -----------------------------------------------------------------
NEXT_PUBLIC_SITE_URL="https://www.montra.cz"

# --- Admin ----------------------------------------------------------------
# Generate with:  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SESSION_SECRET=""
# Generate with:  npm run admin:hash -- "your-password"
ADMIN_PASSWORD_HASH=""

# --- Notifications --------------------------------------------------------
# console | smtp   (SMS provider not selected yet -> NotificationService no-ops)
NOTIFICATION_EMAIL_TRANSPORT="console"
NOTIFICATION_FROM="beroun@montra.cz"
SMTP_URL=""

# --- Payments -------------------------------------------------------------
# cash | (card provider not selected yet)
PAYMENT_PROVIDER="cash"
